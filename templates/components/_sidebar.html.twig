<aside class="blog-sidebar">
    <div class="widget sidebar-widget widget_categories">
        <h4 class="widget-title">Catégories</h4>
        <ul class="list-wrap">
            {% for category in categories %}
                <li>
                    <a href="{{ path('default_category', {'slug': category.slug}) }}">
                        {{ category.name }}
                    </a>
<<<<<<< HEAD
                    <span class="float-right">({{ categoryPostCounts[category.name] }})</span>
=======
                    <span class="float-right">({{ category.posts|length }})</span>
>>>>>>> 01b70c588d287033252263c804b8b8175967d92b
                </li>
            {% endfor %}
        </ul>
    </div>

<<<<<<< HEAD
    <div class="widget sidebar-widget widget_categories">
        <h4 class="widget-title">Articles récents</h4>
        <ul class="list-wrap">
            {% for post in posts %}
                <li>
                    <a href="{{ path('default_post', {
                        'slug': post.slug,
                        'category': post.categories[0].slug
                    }) }}">
                        <img src="{{ asset('uploads/posts/' ~ post.image) }}" alt="{{ post.title }}">
                        {{ post.title }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    </div>
=======
    <div class="widget sidebar-widget">
        <div class="sidePost-active">
            {% for post in posts %}
                <div class="sidePost__item" data-background="{{ asset('uploads/posts/' ~ post.image) }}">
                    <div class="sidePost__content">
                        {% for category in post.categories %}
                            <a class="tag" href="{{ path('default_category', { 'slug' : category.slug }) }}">
                                {{ category.name }}
                            </a>
                        {% endfor %}
                        <h5 class="title tgcommon__hover">
                            <a href="{{ path('default_post', {
                                'slug': post.slug,
                                'category': post.categories[0].slug
                            }) }}">{{ post.title }}</a></h5>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

>>>>>>> 01b70c588d287033252263c804b8b8175967d92b
</aside>